import calendar
from types import new_class


class calenser:
    calendar=new_class('Calendar')
    calender=calender()
    calender.calendar()
    for calendar in calender.calendar:
        calender.formatted=calendar.formatted
        calender.title=calendar.title
         calender.description=calendar.description
        print(calender.formattedlength())
    while cal.not in textwrap.wrap(calendar.title):
        calendar.month=new_class(calendar.month)
        calender.headingline("month")
    datetime.calender(calender.formattedlength)
    if(calendar.day_name not in textwrap.wrap(calendar.title)):
        calendar.day_name=calendar.day_name
        print(calendar.day_name)
    calender.sidedropdown= new_class('Sidedropdown')
if drop:
    print(calendar.sidedropdown)
else:
    print("dropdown not found")
    
